<form method="post" action="{{ path('app_entrainement_delete', {'id': entrainement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ entrainement.id) }}">
    <button class="btn">Delete</button>
</form>
