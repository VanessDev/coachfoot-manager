{% extends 'base.html.twig' %}

{% block title %}Modifier un carton{% endblock %}

{% block body %}
<section class="form-page">
    <div class="form-card">
        <h1>Modifier le carton</h1>

        {{ include('carton/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

        <a href="{{ path('app_carton_index') }}" class="back-link">
            Retour à la liste
        </a>

        {{ include('carton/_delete_form.html.twig') }}
    </div>
</section>
{% endblock %}